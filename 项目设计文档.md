# 项目设计文档 · 历史木管乐器内腔修复证据复核台（task284-boreprobe）

## 1. 目标与用户

面向**乐器修复研究者**的证据复核后端服务：判断一次补片修复是否造成了意外的内腔缩窄和音高偏移。

研究者导入内腔扫描段、补片测量与修复前后频响观测，服务执行尺度校正与段对齐、管腔连通性检查、频响残差比较，并裁决补片影响（无影响 / 缩窄 / 响应冲突）；研究者确认裁决后发布绑定测量基准的冻结快照，最终封存项目作为修复证据版本。

**非禁题类型说明**：主实体是文物乐器的物理测量数据（乐器段、内腔扫描、补片、频响观测），服务对象是研究者做「物理证据与版本复核」，不安排修复任务、不派发负责人、不做流程审批——页面仅复核一个既定方案的物理证据与版本，不属于游戏/桌面工具/业务系统（OA/工单/预约等）类别。

## 2. 业务闭环与状态

**核心闭环（6 步）**：

1. 创建修复项目（记录乐器类型与标称内径）；
2. 导入内腔段 / 补片 / 频响观测（内容哈希幂等防重复）；
3. 执行段对齐（单位统一 mm + 轴向排序 + 重叠矛盾检测）与连通性检查（内径突变断点 / 数据缺口 / 缩窄比）；
4. 比较修复前后频响（基频音分偏移 + 谐振峰残差 + 显著性判定）；
5. 裁决补片影响（缩窄优先 → 响应冲突 → 无影响），研究者确认；
6. 发布冻结快照（绑定测量基准哈希、替代旧版）并封存项目。

**核心状态机（写全）**：

- 修复项目：`pending`（整理中）→ `to_align`（待对齐）→ `to_review`（待复核）→ `published`（已发布）→ `sealed`（封存）；封存后任何修改被拒绝。
- 内腔段：`raw`（原始）→ `aligned`（已对齐）/ `missing`（缺失）/ `anomalous`（异常）→ `excluded`（排除，不参与对齐）。
- 修复影响：`candidate`（候选）→ `none` / `narrowed` / `conflict` → `confirmed`（确认）。
- 修复快照：`draft`（草稿）→ `shared`（共享）→ `frozen`（冻结，绑定基准）→ `superseded`（替代）。

## 3. 持久化与重启恢复

SQLite（modernc.org/sqlite v1.52.0，纯 Go 驱动）保存 8 类数据：`projects`、`segments`、`patches`、`responses`、`connectivity_reports`、`impacts`、`compare_results`、`snapshots`。

- **建表迁移**：`store.Open` 内执行幂等 `CREATE TABLE IF NOT EXISTS`；WAL 模式 + busy_timeout。
- **重启恢复路径**：`--smoke-test` 真实走完「创建项目→导入→对齐→比较→裁决→发布→封存」，随后**关闭数据库并重新打开**，校验项目状态、段数量与快照仍完整存在（自检即重启恢复验证）。
- **断点/游标语义**：段与频响按内容哈希幂等，重复导入返回既有记录；快照冻结时固化测量基准哈希，封存前校验基准一致，不一致拒绝封存。

## 4. API 与模块责任

| 能力 | API 入口 | 生产实现 |
|---|---|---|
| 创建/列表/详情/状态流转项目 | POST/GET /api/projects, PATCH /api/projects/{id}/status | `httpapi/handlers.go → service.CreateProject/UpdateProjectStatus → store/project.go` |
| 导入/列出内腔段 | POST/GET /api/projects/{id}/segments | `httpapi → service.AddSegment → store/segment.go` |
| 段状态标记 | PATCH /api/segments/{id} | `httpapi → service.UpdateSegmentStatus → store/segment.go` |
| 记录/列出补片 | POST/GET /api/projects/{id}/patches | `httpapi → service.AddPatch → store/patch.go` |
| 导入/列出频响 | POST/GET /api/projects/{id}/responses | `httpapi → service.AddResponse → store/response.go` |
| 对齐+连通性检查 | POST /api/projects/{id}/align | `service.AlignAndCheck → bore/align.go + bore/connectivity.go → store/connectivity.go` |
| 内腔轮廓读取 | GET /api/projects/{id}/bore/profile | `service.BoreProfile → bore/align.go` |
| 频响比较 | POST /api/projects/{id}/compare | `service.Compare → acoustic/pitch.go + acoustic/compare.go → store/compare.go` |
| 创建影响候选 | POST /api/projects/{id}/impacts | `service.CreateImpact → bore.PatchInRange + impact/adjudicate.go → store/impact.go` |
| 裁决/确认影响 | PATCH /api/impacts/{id}, POST /api/impacts/{id}/confirm | `service.AdjudicateImpact/ConfirmImpact → store/impact.go`（乐观锁） |
| 发布/列表/读取快照 | POST /api/projects/{id}/snapshots 等 | `service.PublishSnapshot → snapshot/publish.go → store/snapshot.go` |
| 封存项目 | POST /api/projects/{id}/seal | `service.SealProject → snapshot.VerifyBaseHash → store/snapshot.go` |
| 统计/健康/页面 | GET /api/projects/{id}/stats, /api/health, GET / | `httpapi/web.go`（腔体剖面 SVG 渲染） |

## 5. 关键不变量、并发和错误边界

- **唯一键/幂等**：`segments.hash`、`responses.hash` 唯一；段与频响按内容哈希幂等（防重复导入）；测量基准哈希由全部段/频响/补片摘要稳定拼成。
- **不可变快照**：`frozen` 快照绑定基准哈希，除被新快照替代外不可变更；`sealed` 项目拒绝一切写入。
- **状态机流转约束**：封存后状态不可再流转；裁决须为 none/narrowed/conflict（candidate/confirmed 非法目标）；快照仅可 draft→shared→frozen→superseded。
- **并发**：SQLite 单连接串行化；影响裁决用版本号乐观锁（`UPDATE ... WHERE version=?`，影响行数 0 → ErrVersionStale）。
- **错误边界**：尺度单位不明（非 mm/cm/m）→ ErrScaleUnknown；段轴向重叠且内径偏差 >5% → ErrOverlapContradict；补片区间超出轮廓覆盖 → ErrPatchOutOfRange；全部映射为统一 JSON 错误信封与 4xx/409 状态码。

## 6. 端到端验收场景

与 `--smoke-test` 对齐的验收场景（「补片邻接处出现内径缩窄，显示对应音高偏移；研究者确认影响并发布修复快照」）：

1. 创建项目「巴洛克双簧管首修复核」（oboe，标称 14.5mm）→ 状态 pending；
2. 导入上节/下节两段内腔扫描（下节 70–90mm 处内径 12.5mm）→ 幂等、状态 to_align；
3. 导入补片（70–90mm，欧洲枫木 1.2mm）与修复前后频响（440Hz → 442Hz，+7.8 音分）；
4. POST /align → 连通性报告缩窄比 ≈0.862 < 0.9，断言「显著缩窄」；
5. POST /compare → 基频偏移显著（>5 音分）；
6. POST /impacts（补片 ID）→ 候选证据为缩窄；PATCH 裁决为 `narrowed` 再 confirm → `confirmed`；
7. POST /snapshots → 冻结快照，状态 frozen，旧快照被替代；
8. 关闭并重开数据库 → 项目状态 published、段 2 条、快照存在（重启恢复断言）；
9. POST /seal → sealed；封存后新增补片被拒绝（错误边界断言）。
